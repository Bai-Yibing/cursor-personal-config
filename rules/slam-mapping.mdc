---
description: 视觉 SLAM/占据建图调参与验收硬约束（方法论）
globs: "**/*{slam,mapping,map,odom,vo,vio,occupancy}*"
alwaysApply: false
---

# 建图 / SLAM

- 先定传感器拓扑与同步，再调参；前端跟踪与后端建图/闭环参数分离（Ignored 日志即硬信号）。
- **位姿 KPI 与占据 KPI 分表**：lost%/loops 与墙体块状度不可互相替代；积分 path 与节点平面包围盒必须同时看（一长一短都不是整房图）。会话标签 `mapped_with_loop` 不能当整房完成。
- 稀疏近平面雷达不要按多线演示验收；2D 占用默认只吃激光，深度飞点不进栅格。占用/已知低不是墙线成功（扇骨/射线图仍失败）。先积帧再切假扫描。不要对积帧稠密云或碎点 `/map` 做全局闭运算来「修家具」。紧耦合里程计站立稳必须再走对照本体 odom。
- 验收看闭环后拓扑与可导航性；覆盖率仅辅助（警惕开环位姿投影）。
- 无纹理/透明表面走语义旁路；禁止外观 mask 进 VO；禁止语义标 free。
- 长失锁禁随意 reset；防跳变 ≠ 可继续探索；未 recovered 则停扫。
- 语义扩边必须 remap 投票，禁止静默整表清票。详情见 `visual-slam-mapping` / `semantic-occupancy-fusion` / `field-validation-method`。
- 产物路径仅 `<maps_output>/<session>/`。
