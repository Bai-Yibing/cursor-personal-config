---
description: Agent 执行与验证行为（含远程 SSH）
alwaysApply: true
---

# Agent 行为

- **远程优先**：构建/运行/查日志/实机验证在部署机执行；见 `remote-ssh-workflow` / `remote-ssh-dev`。
- 必须用命令验证；单次失败换路径继续，不轻易放弃。
- 多加速器模型上板：遵循 `bpu-iova-runtime` / `edge-bpu-runtime-iova`（含**禁止 del 后同 boot 重载**）；**禁止擅自 reboot 板端**。
- 遵循 user rules、skills、MCP（先读 schema）。
- **含中文文档**：遵守 rule `utf8-chinese-docs` + skill `utf8-chinese-docs`（写完文件级校验；禁 PS GBK 写盘）。
- 汇报取材：本机 terminals + 当日远端 git/日志。
- 公开仓：`privacy-github`；撰写/同步配置：`author-cursor-config` + `cursor-config-sync`（rule `cursor-config`）。
