---
description: 边缘 BPU/NPU 多模型 IOVA 与加载序硬约束
alwaysApply: true
---

# BPU 多模型运行时（IOVA）

上板加载 ≥2 个加速器模型，或出现 `iova addr not equal` / 假「model corrupted」时，遵循 skill **`edge-bpu-runtime-iova`**。

硬约束：

1. **先契约后推理**：同进程要用的模型，在任何 `run`/prefill/decode **之前**按已验证顺序全部加载并绑核。
2. **兄弟段同预载**：同会话后续会用到的 peer（如 Talker+MTP、Enc+Dec）必须在**任一 peer 首次推理前**全部 load；禁止「先推理 A 再晚加载 B」（即使当前模式表面上不用 B）。
3. **一次加载、进程内常驻**：禁止 `del` 后同 boot 再构造加载；工厂方法须缓存实例。多模式要么复用已加载集合，要么每模式独立冷启动。
4. **核是预算**：共享核业务视为**互斥会话**，切换须用户冷启动。
5. **评测隔离**：禁止 encoder-only→dual-load；禁止模式间卸载/晚加载 AR 栈。
6. **毒化即停**：md5 不变仍 corrupted / 新进程也失败 → 停测，向用户说明。
7. **禁止擅自 reboot**：未经用户明确要求，不得 reboot/shutdown。
8. **加载成功 ≠ 任务达标**：IOVA 与精度门禁分列（`horizon-bpu-ptq` / `field-validation-method`）。
