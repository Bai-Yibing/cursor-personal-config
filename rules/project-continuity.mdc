---
description: 跨会话项目状态、决策和故障证据的持续维护
alwaysApply: true
---

# 项目连续性

- 开始非琐碎项目任务前，若存在 `AGENTS.md`、`docs/PROJECT_STATE.md` 或相关 `docs/investigations/`，先读取并遵守；不存在时，仅在需要持续协作、跨会话交接或用户要求跟进时按 `project-continuity` 建档。
- 将当前状态与历史证据分层：`PROJECT_STATE.md` 只保留仍有效的摘要；关键决策放 `docs/decisions/`；可复现的故障调查放 `docs/investigations/`。不把完整日志、凭据、IP、隐私数据写入这些文件。
- 完成会改变架构、行为、验证结论或后续行动的工作后，更新受影响的项目记录；写明事实、证据位置、结论边界与下一步。没有证据时明确标为待验证，不得编造。
- 已否证的方案必须记录失败条件和替代路径，开始相似工作前先检查，避免重复尝试。
- 不为一次性小改动制造文档噪声；状态文件超出当前工作所需时，压缩已完成事项为可追溯的简史。
