---
description: Hard constraints when authoring Cursor skills and rules
globs: "**/.cursor/**/*"
alwaysApply: false
---

# Skills / Rules 撰写约束

触发：新增或改写 skills/rules，或用户问「怎么写 skill/rule」。详见 `author-cursor-config`。

## 必须

1. **抽象为普适方法论**（决策树/门禁/故障分类/反模式+理由），不写单项目菜谱主轴。
2. **遵守 `privacy-github`**：无 IP/凭据/个人路径；用占位符。
3. Skill 必有 YAML frontmatter（`name` + 英文 `description` 含 WHEN）；领域类优先 9 段骨架。
4. Rule 保持短硬约束；长 SOP 放入 skill。
5. 增删后更新根目录 `sync-manifest.json`，再 publish/push + install。
6. Windows 下用 conda Python 以 UTF-8 写入并验证中文可读。
7. Remote-SSH：真源是 `~/.cursor-personal-config`，不是项目 `.cursor` 副本。

## 禁止

- 把日报/实机原始数据贴进 skills
- 缺少理由的纯禁令堆积
- 只改项目 `.cursor` 副本而不更全局真源（除非用户明确要求）
8. 远端/板端发布与桥接推送：遵循 `cursor-config-remote-sync`（真源为 git 仓；无凭据时用 bundle）。
