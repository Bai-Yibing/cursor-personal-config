---
description: 边缘 NPU/BPU PTQ 与上板门禁（方法论）
globs: "**/*.{hbm,onnx,yaml,yml}*"
alwaysApply: false
---

# NPU / PTQ 上板

- 优先级：全加速器门禁（CPU/hybrid=0）→ 任务精度 → 墙钟；不为 fp16/双核牺牲门禁。
- 交付：产物落盘 + 成功日志；进度 UI 不算完成；中断保 `.bc` 可续编。
- 能加载 ≠ 可上线：profiler + 板端延迟与任务**多指标**；禁止只盯单点校准 cosine。
- **校准卫生**：域对齐部署；**calib ∩ held-out = ∅**；禁止默认 randn pad；host/board/float **同 feed** 才可比。
- **分层验收**：oracle/条件路径 ≠ freerun/开放域质量；联调 e2e_ok ≠ 语义正确；board≈float 时停同域 PTQ。
- **先契约后重编**：mask/dtype/prefill/`input_source` 与导出一致后再判量化；勿把契约病当 PTQ 病盲编。
- 整图不支持则先改图再 PTQ；静态边界从**在线调用图**推导；静态图与 LLM runtime 分离；一长编译一容器/GPU。
- 新位宽/配方须双路径门禁过才替默认包；失败立即 rollback。
- 多核只给算力墙段且量化配方不变；墙钟含 host/DDR 时勿误判为算力不足。
- **多模型同 SoC**：加载序 / **兄弟段同预载** / 实例常驻 / 互斥会话见 rule `bpu-iova-runtime` 与 skill `edge-bpu-runtime-iova`（禁止推理后再晚加载 peer；毒化勿擅自 reboot）。
- **任务隔离**：`layout_ensure <TASK>`；禁止跨 task 拷贝 ONNX/HBM/calib 当依赖；SoC 按 march 分编。
- 工作目录仅 `<ptq_workspace>`；模型资产 / 校准日志交付包分 `<models_root>` 与 `<data_root>`（见仓库 `STORAGE_LAYOUT` / rule `ptq-storage-layout`）。
- 主机 Python：每 `task/<TASK>/.venv` 独立；禁止根目录 `.venv`/`.venv_oellm` 与 task 间软链；OELLM 用项目内 CPython + `venv --copies`，禁止外置宿主机还原树。详情见 `horizon-bpu-ptq` / `field-validation-method`。
