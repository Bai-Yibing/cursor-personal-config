---
description: Horizon/S600 BPU 量化与上板门禁
globs: "**/*.{hbm,onnx,yaml,yml}*"
alwaysApply: false
---

# BPU / PTQ 上板

- 交付门禁：目标子图 CPU segment = 0（无 hybrid）；HBM 落盘 + 成功日志；进度 100% 不算完成。
- 能量化加载不等于可上线：查 profiler 与端到端 ms；ConvTranspose 落 CPU 可到数秒/帧。
- 大模型拆段或 System1(HBM)+System2(LLM)；勿赌单 HBM 塞满 DualVLN。
- 校准/kl/敏感 fp16 可能毁门禁或精度；保留带时间戳回退包；板上指标未测不宣称提升。
- 细节见 skill `horizon-bpu-ptq`。
