---
description: 默认在远程 Linux 服务器上通过 SSH 开发与验证
alwaysApply: true
---

# 远程 SSH 工作环境

用户**主要工作场景**：本机 Windows 运行 Cursor，代码构建、运行、实机测试在**远程 Linux** 上完成。

## 执行位置判定

| 情况 | 命令在哪跑 |
|------|------------|
| Cursor 已 Remote-SSH 连到远端，工作区在 Linux | **直接在集成终端执行**（已是远端 shell） |
| 工作区在本机 Windows，操作目标在服务器 | `ssh <Host别名> '命令'` 或 `ssh <Host别名>` 进交互 |
| 不确定 | 先看终端 `cwd`、对话上下文、用户提到的主机名 |

## 常用 SSH 主机（settings 已配置）

`S100`、`S100-SSH`、`Go2-SSH`、`192.168.1.56`、`47.116.133.221`；另常见内网 GPU 量化机、XC100 板端（均为 linux）。

汇报与知识库中写清：**哪台机器、什么路径、什么时间**（BPU 另加容器/GPU）。

## 开发与验证

- 改代码前确认改的是**本地副本还是远端工作区**；避免只改一端未同步。
- 构建/测试/查日志在**实际运行环境**（远端）执行，不单靠本机推断。
- 长任务用远端 `tmux`/`screen`；断连后 `tmux attach` 续查。
- 路径：远端用 Linux 路径（`/root/...`）；汇报存档用 `D:\Documents\`（本机）。

## 打开任意 SSH 工作区

个人 Cursor 配置走 **Git**，不绑定某台主机或 `/root/vln`：

1. 每台 Linux 一次：`git clone <repo> ~/.cursor-personal-config`
2. 每个项目一次：`~/.cursor-personal-config/scripts/install-to-project.sh <项目根>`
3. 本机改配置：`publish-cursor-config.ps1` → `git push`

详见 `cursor-personal-config/README.md` 与 skill `sync-cursor-to-project`。

## 材料搜集（写日报/知识库时）

除本地 transcripts、terminals 外，必须纳入**当日涉及的远程主机**上的 git、日志、测试输出。详见 skill `remote-ssh-dev` 中的 `remote-materials.md`。
