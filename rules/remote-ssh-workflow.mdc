---
description: 默认在远程 Linux 上通过 SSH 开发与验证
alwaysApply: true
---

# 远程 SSH 工作环境

本机 Windows 运行 Cursor；**构建/运行/实机验证在远端 Linux**。详见 `remote-ssh-dev`。

## 执行位置

| 情况 | 命令在哪跑 |
|------|------------|
| Remote-SSH，工作区在 Linux | 直接在集成终端执行 |
| 工作区在 Windows | `ssh <Host> '命令'` |
| 不确定 | 看 `cwd`、对话上下文、主机角色 |

## 角色主机（禁止写真实 IP）

`<perception_host>` / `<robot_host>` / `<ptq_host>` / `<Host>`

汇报写清：**主机角色、路径占位符、时间**。

## 开发与验证

- 构建/测试/日志在**实际运行环境**执行。
- 长任务用远端 `tmux`/`screen`。
- 路径：远端 `<project_root>/...`。

## 个人 Cursor 配置

项目 `.cursor` 是 install 副本。持久化/推送/桥接见 skill `cursor-config-sync`（勿只改项目副本就结束）。

## 材料收集

本机 transcripts/terminals + 当日远端 git/日志。见 `remote-ssh-dev` → `remote-materials.md`。
